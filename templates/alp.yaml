os:
  name: alpine
description: A base alpine container, with several packages, but minimal processes
include:  
  - include/template.yaml
  - ../device/standard.yaml
  - ../device/apk-cache.yaml
  - ../local/alpine/template.yaml
  - ../local/alpine/packages.yaml
  - ../alpine/base/alpine.yaml
stop: true  
snapshot: copy  

