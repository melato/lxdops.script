os:
  name: alpine
description: |
  A base alpine container, with several packages, but minimal processes.
  Can replace alp.yaml as the base alpine template.
include:  
- include/alpine.yaml
- ../alpine/packages/packages.yaml
stop: true  
snapshot: copy  

