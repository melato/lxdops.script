os:
  name: alpine
description: create a user, using the name, uid, gid of the host user
users:
    - name:
      shell: /bin/bash
      sudo: true
      ssh: true
      groups:
        - wheel
        - adm
