description: |
    copy files from /etc/opt/etc/ to /etc/, and reboot
    obsolete: use files.yaml instead
scripts:
  - name: etc
    reboot: true
    body: |
      if [ -d /etc/opt/etc ]; then
        cp -R /etc/opt/etc /
      fi
